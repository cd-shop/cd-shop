

<%= form_for(@genre, url:"/admin/products") do |genre|%>
    <%= genre.text_field :genrename %>
    <%= genre.submit %>
<% end %>

<%= form_for(@label, url:"/admin/products") do |label|%>
    <%= label.text_field :labelname %>
    <%= label.submit %>
<% end %>

<%= form_for(@artist, url:"/admin/products") do |artist|%>

    <%= artist.text_field :artistname %>
    <%= artist.submit %>

<% end %>